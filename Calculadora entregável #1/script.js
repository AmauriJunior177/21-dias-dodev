

function calculadora(num1, num2, operacao) {
    if (operacao == '+'){
        return num1 + num2
    }else if (operacao == '-'){
        return num1 - num2
    }else if (operacao == '*'){
        return num1 * num2
    }else if (operacao == '/'){
        return num1 / num2 
    };

calculadora(524,555,'/')}